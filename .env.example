# Copy to .env and fill in. .env is gitignored — it will never be committed.
#   cp .env.example .env && chmod 600 .env
PROXY_API_KEY='sk-...'

# Optional overrides (defaults shown):
# PROXY_BASE_URL='https://models.aikin.club'
# AGENT_MODEL='deepseek-v3.2'
# JUDGE_MODEL='claude-sonnet-4-6'
# SHARDS=1                       # 24 is safe on a strong machine (~2.5h full run)
